Smoke-test outputs land here (`*.converted.md` is gitignored).

```bash
bash ../skills/everything-to-markdown/scripts/convert.sh /path/to/file.pdf --mode skip-text \
  -o "$(pwd)/out.converted.md"
```
